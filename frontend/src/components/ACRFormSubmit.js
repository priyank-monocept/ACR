import React from 'react'
import useFormApi from "../utils/useFormApi";

const ACRFormSubmit = (props) => {
    console.log(props);
    //useFormApi(data);
  return (
    <div>ACRFormSubmit</div>
  )
}

export default ACRFormSubmit
